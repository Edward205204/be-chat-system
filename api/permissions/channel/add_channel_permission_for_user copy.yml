info:
  name: add_channel_permission_for_user copy
  type: http
  seq: 6

http:
  method: PUT
  url: "{{path}}/servers/{{serverId}}/channels/{{channelId}}/permissions/users/{{memberId}}"
  body:
    type: json
    data: |-
      {
        "permission": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
