info:
  name: get_permissions_of_channel
  type: http
  seq: 1

http:
  method: GET
  url: "{{path}}/servers/{{serverId}}/channels/{{channelId}}/permissions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
