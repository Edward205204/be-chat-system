info:
  name: remove_channel_permission_for_role
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{path}}/servers/{{serverId}}/channels/{{channelId}}/permissions/roles/{{roleId}}/permission/{{permission}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
