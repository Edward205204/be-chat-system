info:
  name: get_permissions_of_role
  type: http
  seq: 1

http:
  method: GET
  url: "{{path}}/servers/{{serverId}}/roles/{{roleId}}/permissions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
