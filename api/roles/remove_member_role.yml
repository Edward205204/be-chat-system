info:
  name: remove_member_role
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{path}}/servers/{{serverId}}/roles/{{roleId}}/members/{{memberId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
