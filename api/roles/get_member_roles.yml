info:
  name: get_member_roles
  type: http
  seq: 4

http:
  method: GET
  url: "{{path}}/servers/{{serverId}}/roles/{{roleId}}/members"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
