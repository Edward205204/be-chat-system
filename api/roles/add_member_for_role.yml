info:
  name: add_member_for_role
  type: http
  seq: 5

http:
  method: POST
  url: "{{path}}/servers/{{serverId}}/roles/{{roleId}}/members"
  body:
    type: json
    data: |-
      {
        "memberId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
