info:
  name: get_roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{path}}/servers/{{serverId}}/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
