info:
  name: search_user
  type: http
  seq: 3

http:
  method: GET
  url: "{{path}}/users/search?q={{userIdOrEmail}}"
  params:
    - name: q
      value: "{{userIdOrEmail}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
