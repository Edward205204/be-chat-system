info:
  name: get_user_with_id
  type: http
  seq: 4

http:
  method: GET
  url: "{{path}}/users/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
