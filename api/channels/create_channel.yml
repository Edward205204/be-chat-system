info:
  name: create_channel
  type: http
  seq: 2

http:
  method: POST
  url: "{{path}}/servers/{{serverId}}/channels"
  body:
    type: json
    data: |-
      {
        "name": "",
        "isPrivate": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
