info:
  name: channel_patch_update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{path}}/servers/{{serverId}}/channels/{{channelId}}"
  body:
    type: json
    data: |-
      {
        "name": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
