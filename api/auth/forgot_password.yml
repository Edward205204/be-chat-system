info:
  name: forgot_password
  type: http
  seq: 7

http:
  method: POST
  url: "{{path}}/auth/send/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": "nguyentminhkhoa1@gmail.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
