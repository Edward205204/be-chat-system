info:
  name: reset_password
  type: http
  seq: 8

http:
  method: POST
  url: "{{path}}/auth/reset-password"
  body:
    type: json
    data: |-
      {
        "email": "nguyentminhkhoa1@gmail.com",
        "password": "Mk@1231234",
        "otp": "160691"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
