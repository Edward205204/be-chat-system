info:
  name: delete_server
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{path}}/servers/{{serverId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
