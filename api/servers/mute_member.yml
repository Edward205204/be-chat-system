info:
  name: mute_member
  type: http
  seq: 16

http:
  method: PATCH
  url: "{{path}}/servers/{{serverId}}/members/{{kickMemberId}}/mute"
  body:
    type: json
    data: |-
      {
        "mute": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
