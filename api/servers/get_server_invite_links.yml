info:
  name: get_server_invite_links
  type: http
  seq: 10

http:
  method: GET
  url: "{{path}}/servers/{{serverId}}/links"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
