info:
  name: ban_member
  type: http
  seq: 8

http:
  method: POST
  url: "{{path}}/servers/{{serverId}}/bans/{{banMemberId}}"
  body:
    type: json
    data: |-
      {
        "reason": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
