info:
  name: get_serverbans
  type: http
  seq: 7

http:
  method: GET
  url: "{{path}}/servers/{{serverId}}/bans"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
