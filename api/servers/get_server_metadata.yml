info:
  name: get_server_metadata
  type: http
  seq: 4

http:
  method: GET
  url: "{{path}}/servers/{{serverId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
