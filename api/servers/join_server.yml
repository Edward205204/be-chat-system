info:
  name: join_server
  type: http
  seq: 3

http:
  method: POST
  url: "{{path}}/servers/join/{{inviteToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
