info:
  name: servers
  type: folder
  seq: 3

request:
  auth: inherit
  scripts:
    - type: before-request
      code: |-
        console.log("PRE REQUEST START");

        try {
            const res = await bru.sendRequest({
                method: "POST",
                url: "http://localhost:8080/rchat/auth/login",
                headers: {
                    "Content-Type": "application/json"
                },
                data: {
                    email: "khoa123kd56cn@gmail.com",
                    password: "Mk@123123"
                }
            });

            console.log(res);

            const token = res.data.result.accessToken;

            bru.setEnvVar("accessToken", token);

        } catch (err) {
            console.log(err);
        }
