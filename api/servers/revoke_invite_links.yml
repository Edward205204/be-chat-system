info:
  name: revoke_invite_links
  type: http
  seq: 12

http:
  method: PATCH
  url: "{{path}}/servers/{{serverId}}/links/{{linkId}}/revoke"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
