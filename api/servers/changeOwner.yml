info:
  name: changeOwner
  type: http
  seq: 17

http:
  method: PATCH
  url: "{{path}}/servers/{{serverId}}/owner/{{newOwnerId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
