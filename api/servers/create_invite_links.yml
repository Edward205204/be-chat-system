info:
  name: create_invite_links
  type: http
  seq: 11

http:
  method: POST
  url: "{{path}}/servers/{{serverId}}/links"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
